[build]
  command = "git submodule update --init --recursive && hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.138.0"
  HUGO_EXTENDED = "true"

[dev]
  command = "hugo server"
  publish = "public"
  port = 1314
